<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProductMapper">
 	
 	
	<resultMap id="productSelectMap" type="product">
		<result property="prodNo" 		column="prod_no" 						jdbcType="INTEGER"/>
		<result property="prodType"	column="prod_type" 				jdbcType="VARCHAR" />
		<result property="prodName"	column="prod_name" 				jdbcType="VARCHAR" />
		<result property="prodContent" 	column="prod_content" 				jdbcType="VARCHAR" />
		<result property="prodImage" 	column="prod_image" 		jdbcType="VARCHAR" />
		<result property="price" 			column="price" 							jdbcType="INTEGER" />
		<result property="prodQuantity" 		column="prod_quantity" 					jdbcType="INTEGER" />
		<result property="regDate" 		column="reg_date" 					jdbcType="DATE" />
		<result property="deleteFlag" column="delete_flag"							jdbcType="VARCHAR" />
	</resultMap>
	
	<!-- SQL : INSERT -->
	<insert 	id="addProductAdmin"		parameterType="product" >
	 	INSERT
		INTO product (prod_no, prod_type, prod_name, prod_content, prod_image, price, prod_quantity, reg_date) 
		VALUES (seq_product_prod_no.NEXTVAL, ${prodType}, #{prodName}, #{prodContent}, #{prodImage}, #{price}, #{prodQuantity}, SYSDATE)
	 </insert>
	 
 	 <!-- SQL : SELECT ONE -->
	 <select 	id="getProduct"	parameterType="int"	resultMap="productSelectMap">
		SELECT
		prod_no, prod_type, prod_name, prod_content, prod_image, price, prod_quantity, reg_date
		FROM product
		WHERE prod_no = #{value}
	 </select>
	 
 	 <!-- SQL : UPDATE -->
	 <update	id="updateProductAdmin"	parameterType="product" >
	   	UPDATE product
	   	<set>
	   		prod_type = #{prodType},
	   		prod_name = #{prodName},
	   		price = #{price},
	   		prod_image = #{prodImage, jdbcType=VARCHAR},
	   		prod_content = #{prodContent, jdbcType=VARCHAR},
	   		prod_quantity = #{prodQuantity}
	   	</set>
	   	WHERE prod_no = #{prodNo}
	 </update>
	 
	<!-- SQL : DELETE -->
	 <update	id="deleteProductAdmin"	parameterType="product" >
	   	UPDATE product
	   	<set>
	   		delete_flag = #{deleteFlag}
	   	</set>
	   	WHERE prod_no = #{prodNo}
	 </update>
	 
		 
 	<!-- SQL : SELECT LIST -->
	<select  id="getProductList"  parameterType="search"	resultMap="productSelectMap">
		SELECT * 
		FROM ( SELECT inner_table.* , ROWNUM AS row_seq
	  				 FROM ( SELECT prod_no, prod_type, prod_image, prod_name, price
	  				 FROM product
	  				 <where>
		  				 <if test="searchCondition != null">
		  				 		prod_name LIKE '%'||#{searchKeyword}||'%'
		  				 </if>
	  				 </where>
	  				 ORDER BY
	  				 <if test="prodSort == 0">
	  				 	reg_date DESC
	  				 </if>
	  				 <if test="prodSort == 1">
	  				 	price
	  				 </if>
	  				 <if test="prodSort == 2">
	  				 	price
	  				 </if>
	  				 <if test="prodSort == 3">
	  				 	price DESC
	  				 </if> ) inner_table
	  				 WHERE ROWNUM &lt;= #{endRowNum} )
		WHERE row_seq BETWEEN #{startRowNum} AND #{endRowNum}	
	 </select>

	
 	<!-- SQL : SELECT ROW Count -->
	 <select  id="getTotalCount"  parameterType="map"	 resultType="int">
	 	SELECT COUNT(*)
	 	FROM ( SELECT prod_no, prod_type, prod_image, prod_name, price
	  				 FROM product
	  				 <where>
	  				 	<if test='prodType=="0" '>
	  				 		prod_type = '0'
	  				 	</if>
	  				 	<if test='prodType=="1" '>
	  				 		prod_type = '1'
	  				 	</if>
		  				 <if test="a.searchCondition != null">
		  				 	<if test="a.searchKeyword !='' ">
		  				 		prod_name LIKE '%'||#{searchKeyword}||'%'
		  				 	</if>
		  				 </if>
	  				 </where> ) countTable				
	 </select>
	 
	 
	 
	 
</mapper>